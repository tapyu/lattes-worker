  Esse repo contém os arquivos necessários para gerar a imagem Docker que é utilizada posteriormente para dar um deploy em um host (no nosso caso, Render).
O Rencer, por sua vez, cria um container (uma instância em execução dessa imagem) que funciona como um servidor responsável por executar microserviços automatizados de interação com a Plataforma Lattes.
